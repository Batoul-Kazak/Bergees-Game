import { PieceIndexType, selectedPlayerAvailableCellType } from "../../types/ChessGame/ChessGameTypes";

export const getKnightLogic = (
  piece: PieceIndexType,
  whitePieces: PieceIndexType[],
  blackPieces: PieceIndexType[]
): selectedPlayerAvailableCellType[] => {
  // ... logic here
  return [];
};